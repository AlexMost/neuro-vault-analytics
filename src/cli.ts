#!/usr/bin/env node
/**
 * nv-analytics — placeholder entrypoint.
 *
 * The real implementation is tracked by the spec at
 * docs/superpowers/specs/2026-04-26-usage-analytics-workflow-design.md.
 * This stub exists so `npm run build` produces a valid dist/cli.js for the
 * plugin manifest to point at; it intentionally exits non-zero with a clear
 * message so accidental invocations during scaffold are obvious.
 */

console.error(
  'nv-analytics: not implemented yet. See docs/superpowers/specs/2026-04-26-usage-analytics-workflow-design.md',
);
process.exit(2);
